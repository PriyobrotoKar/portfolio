---
import { cn } from "@/lib/utils";
const { className } = Astro.props;
---

<h2
  class={cn(
    " leading-tight w-fit mx-auto max-w-screen-lg bg-gradient-to-t from-neutral-600 to-white bg-clip-text text-transparent",
    className
  )}
>
  <slot />
</h2>
